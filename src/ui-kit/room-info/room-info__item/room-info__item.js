import './room-info__item.scss'

export default class RoomInfoItem {
	constructor(title, description, infoIcon) {
		this.title = title;
		this.description = description;
		this.infoIcon = infoIcon
	}
}
